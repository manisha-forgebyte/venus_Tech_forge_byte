modules/

Feature modules live here. Each business area should keep its controller,
service, DTOs, and local entity/view-model types together.

Current active modules:
- auth
- assets
- companies
- compatibility
- entities
- filings
- market
- reserve
- users

The compatibility module is the main bridge for the existing Angular frontend
because it preserves the legacy /api/{Module}/{Action} route pattern.
