Prisma migration history belongs in this folder.

This project currently uses the checked-in Prisma schema as the source of
truth. When schema migrations are introduced, generate them with Prisma so the
folder contains timestamped migration directories rather than hand-written
SQL fragments.
